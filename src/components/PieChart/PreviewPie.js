import * as d3 from "d3"

export class PreviewPie{
  //params:
    /**
     * {
     *  measurements: {startAngle, endAngle, min_radius, max_radius},
     *  config: {direction, initial},
     *  index,
     *  data,
     *  svg,
     *  previewLocation
     * }
     * 
     */
  

    /**
     * PieData:
     * 
     * - Measurements:
     *    - Radius: init, target, current
     *    - Angle: start, end
     * - Temporary / Full component
     * - Visuals:
     *    - Name
     *    - Color
     */

    /**
     * Controller spawns in
     * 
     */
}